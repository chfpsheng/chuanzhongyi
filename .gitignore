# 站点配置：含数据库地址、库名、密码明文，以及米拓安全码
# 本仓库为公开仓库，绝不能纳入版本库。
# 2026-09-21 已用 `git rm -r --cached config/` 从跟踪中移除（本地文件保留，不影响运行）。
# 部署到服务器请用 FTP 上传代码，config/ 由服务器本地保留，不要覆盖。
/config/

# 运行时缓存（栏目/配置/模板编译缓存）
/cache/
/templates/*/cache/

# 自动生成的缩略图
/upload/thumb_src/

# 编辑器本地数据
/.codebuddy/

# 数据库备份：含全库结构与数据，本仓库为公开仓库，不纳入版本库
/admin/databack/*.sql
/admin/databack/*.db
/_backup/

# 临时/备份文件
*.bak
*.zip
